import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Importa el componente App

ReactDOM.render(<App />, document.getElementById('root')); // Renderiza el componente App en el contenedor con el ID 'root'

